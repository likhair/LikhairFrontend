export class Notes {
  constructor({ noteId, title, content,created_date,updated_date }) {
    this.noteId = noteId
    this.title = title
    this.content = content
    this.created_date = created_date
    this.coneupdated_datent = updated_date
  }
}